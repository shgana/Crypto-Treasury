import {Badge} from "../components";
import {money} from "../utils";
async function getData(){const r=await fetch("http://localhost:8000/overview",{cache:"no-store"});return r.json()}
export default async function Overview(){const d=await getData();const max=Math.max(...d.by_asset.map((x:{value:number})=>x.value));const cards=[["Portfolio NAV",money(d.nav),"as of 14:15 UTC"],["Gross digital-asset exposure",money(d.gross_exposure),"Excludes USD cash"],["Open exceptions",d.open_exceptions,`${d.priority_exceptions} high priority`],["Reconciliation health",`${d.reconciliation_rate}%`,`412 records successfully matched`]];return <><header><div><p className="eyebrow">OPERATIONS / OVERVIEW</p><h1>Good afternoon, Avery.</h1><p className="muted">Your synthetic portfolio is reconciled through 14:15 UTC.</p></div><button>Run reconciliation</button></header><section className="metrics">{cards.map(c=><div className="card" key={c[0]}><p>{c[0]}</p><h2>{c[1]}</h2><small>{c[2]}</small></div>)}</section><section className="alert"><div><Badge value="HIGH"/><b> Counterparty concentration</b><p>{d.concentration_warning.message}</p></div><button>Review exposure</button></section><section className="grid"><div className="panel"><div className="panelhead"><div><h3>Exposure by asset</h3><p>Internal ledger value by canonical asset</p></div><span className="muted">USD</span></div><div className="bars">{d.by_asset.map((x:{name:string,value:number})=><div className="barrow" key={x.name}><span>{x.name}</span><i><b style={{width:`${x.value/max*100}%`}}/></i><strong>{money(x.value)}</strong></div>)}</div></div><div className="panel"><div className="panelhead"><div><h3>Control summary</h3><p>Operational state at a glance</p></div></div><div className="summaryrow"><span>Stablecoin exposure</span><b>{money(d.stablecoin)}</b></div><div className="summaryrow"><span>USD cash</span><b>{money(d.cash)}</b></div><div className="summaryrow"><span>Unsettled transfers</span><b>{d.unsettled_transfers}</b></div><div className="summaryrow"><span>Priority exceptions</span><Badge value="HIGH"/></div></div></section><section className="grid"><div className="panel"><div className="panelhead"><div><h3>Exposure by venue</h3><p>Custody and execution concentration</p></div></div>{d.by_venue.map((v:{name:string,value:number})=><div className="summaryrow" key={v.name}><span>{v.name}</span><b>{money(v.value)}</b></div>)}</div><div className="panel"><div className="panelhead"><div><h3>Exception severity</h3><p>Deterministically prioritized breaks</p></div></div>{d.severity_counts.map((x:{name:string,value:number})=><div className="summaryrow" key={x.name}><Badge value={x.name}/><b>{x.value}</b></div>)}</div></section></>}
