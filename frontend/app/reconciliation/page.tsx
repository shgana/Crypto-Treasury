import {Badge} from "../components";
import {money} from "../utils";
async function getItems(){return (await fetch(`${process.env.API_URL||"http://localhost:8000"}/reconciliation-items`,{cache:"no-store"})).json()}
export default async function Reconciliation(){const items=await getItems();return <><header><div><p className="eyebrow">OPERATIONS / RECONCILIATION</p><h1>Reconciliation workspace</h1><p className="muted">Deterministic matching across internal and external synthetic feeds.</p></div><button>Export results</button></header><div className="filters"><button className="active">All statuses</button><button>Breaks</button><button>Exact match</button><button>Exchange A</button><button>Custodian A</button></div><section className="panel tablepanel"><div className="panelhead"><div><h3>Latest reconciliation run</h3><p>Run #1 · 14:15 UTC · quantity tolerance 0.01</p></div><b>98.4% matched</b></div><table><thead><tr><th>Asset</th><th>Internal quantity</th><th>External quantity</th><th>Difference</th><th>USD impact</th><th>Venue</th><th>Status</th><th>Timestamp</th></tr></thead><tbody>{items.map((x:any)=><tr key={x.id}><td><b>{x.asset}</b></td><td>{x.internal_quantity.toLocaleString()}</td><td>{x.external_quantity.toLocaleString()}</td><td className={x.difference ? "negative" : ""}>{x.difference.toLocaleString()}</td><td>{money(x.usd_impact)}</td><td>{x.venue}</td><td><Badge value={x.status}/></td><td>{new Date(x.timestamp).toLocaleTimeString([],{hour:"2-digit",minute:"2-digit"})}</td></tr>)}</tbody></table></section></>}
