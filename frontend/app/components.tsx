"use client";
import {useRouter} from "next/navigation";
export function Badge({value}:{value:string}){return <span className={`badge ${value.toLowerCase().replaceAll("_","-")}`}>{value.replaceAll("_"," ")}</span>}
export function ExceptionActions({id,status}:{id:number,status:string}){const router=useRouter();async function update(data:object){await fetch(`/api/exceptions/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});router.refresh()}return <div className="actions"><button onClick={()=>update({assigned_analyst:"Avery Chen"})}>Assign to me</button>{status!=="RESOLVED"&&<button className="primary" onClick={()=>update({status:"RESOLVED",resolution_notes:"Validated and resolved in demo workflow."})}>Resolve</button>}</div>}
export function CommentForm({id}:{id:number}){const router=useRouter();async function submit(formData:FormData){await fetch(`/api/exceptions/${id}/comments`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({body:formData.get("body")})});router.refresh()}return <form action={submit} className="comment"><input required name="body" placeholder="Add investigation note…"/><button className="primary">Add note</button></form>}
